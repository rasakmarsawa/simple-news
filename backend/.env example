# Environment
NODE_ENV=development
PORT=5000

# Database config
DB_HOST=db
DB_PORT=3307
DB_NAME=simple-news
DB_USERNAME=user
DB_PASSWORD=password
DB_DIALECT=mysql

# MySQL container config (for docker-compose)
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=simple-news
MYSQL_USER=user
MYSQL_PASSWORD=password

# JWT secret
JWT_SECRET=this-is-a-secret

# CORS config (allowed frontend URLs)
CORS_ORIGIN=http://localhost:5173,http://127.0.0.1:5173
